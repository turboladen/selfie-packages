---
name: dotenv-linter
version: "0.1.0"
homepage: 
description: 

_default_environment: &default_env
  install: cargo binstall --no-confirm dotenv-linter
  check: command -v dotenv-linter
  dependencies:
    - cargo-binstall

environments:
  macos-work:
    <<: *default_env
  arch-home:
    <<: *default_env

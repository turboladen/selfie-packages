---
name: tflint
version: "0.1.0"
homepage: https://github.com/terraform-linters/tflint
description: Linter for Terraform files

environments:
  macos-work:
    install: brew install tflint
    check: command -v tflint
    dependencies:
      - homebrew

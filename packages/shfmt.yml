---
name: shfmt
version: 0.1.0
homepage: https://github.com/patrickvane/shfmt
description: A shell formatter (sh/bash/mksh)

environments:
  macos-home:
    install: brew install -v shfmt
    check: command -v shfmt
    dependencies:
      - homebrew

  macos-work:
    install: brew install -v shfmt
    check: command -v shfmt
    dependencies:
      - homebrew

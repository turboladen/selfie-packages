---
name: htmlhint
version: "0.1.0"
homepage: https://github.com/htmlhint/HTMLHint
description: The static code analysis tool you need for your HTML 

environments:
  macos-home:
    install: npm -g install htmlhint
    check: command -v htmlhint
    dependencies:
      - nodejs

  macos-work:
    install: npm -g install htmlhint
    check: command -v htmlhint
    dependencies:
      - nodejs

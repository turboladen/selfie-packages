---
name: gcloud-cli
version: 0.1.0
homepage: 
description: 

environments:
  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask gcloud-cli
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask gcloud-cli
    dependencies:
      - homebrew

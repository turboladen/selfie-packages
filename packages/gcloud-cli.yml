---
name: gcloud-cli
version: 0.1.0
homepage: https://cloud.google.com/cli/
description: Google Cloud Command Line Interface (gcloud CLI)

environments:
  macos-work:
    install: brew install --cask gcloud-cli
    check: brew list --cask gcloud-cli
    dependencies:
      - homebrew

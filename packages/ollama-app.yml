---
name: ollama-app
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask ollama-app
    check: brew list --cask ollama-app
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask ollama-app
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask ollama-app
    dependencies:
      - homebrew

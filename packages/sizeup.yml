---
name: sizeup
version: 0.1.0
homepage: 
description: 

_brew: &brew
  install: brew install --cask sizeup
  check: brew list --cask sizeup
  dependencies: [homebrew]

environments:
  macos-home: *brew
  macos-work: *brew

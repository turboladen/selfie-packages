---
name: claude
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask claude
    check: brew list --cask claude
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask claude
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask claude
    dependencies:
      - homebrew

---
name: nodejs
version: 0.1.0
homepage:
description:

environments:
  macos-work:
    install: |
      fnm install --lts
      fnm use lts-latest
      fnm default lts-latest

    check: |
      command -v node && \
        command -v npm && \
        npm --version
        
    dependencies:
      - fnm

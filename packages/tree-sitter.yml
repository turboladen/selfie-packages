---
name: tree-sitter
version: "0.1.0"
homepage: https://tree-sitter.github.io/
description: Incremental parsing library

environments:
  macos-home:
    install: brew install tree-sitter
    check: brew list --versions tree-sitter
    dependencies:
      - homebrew
  macos-work:
    install: brew install tree-sitter
    check: brew list --versions tree-sitter
    dependencies:
      - homebrew


---
name: pdm
version: "0.1.0"
homepage: https://pdm-project.org
description: |
  (Python Development Master) is a modern package and dependency manager.

_uv_tool_install: &uv_tool_install
  install: uv tool install pdm
  check: command -v pdm
  dependencies: [uv]

environments:
  macos-work: *uv_tool_install

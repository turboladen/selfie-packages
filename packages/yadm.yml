---
name: yadm
version: "0.1.0"
homepage: 
description: 

environments:
  macos-home:
    install: brew install yadm
    check: command -v yadm
    dependencies:
      - homebrew
  macos-work:
    install: brew install yadm
    check: command -v yadm
    dependencies:
      - homebrew


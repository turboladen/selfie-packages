---
name: firefox
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask firefox
    check: brew list --cask firefox
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask firefox
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask firefox
    dependencies:
      - homebrew

---
name: font-powerline-symbols
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask font-powerline-symbols
    check: brew list --cask font-powerline-symbols
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask font-powerline-symbols
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask font-powerline-symbols
    dependencies:
      - homebrew

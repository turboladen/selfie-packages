name: font-powerline-symbols
homepage: https://github.com/powerline/powerline
description: Powerline Symbols

environments:
  macos-home:
    install: brew install --cask font-powerline-symbols
    check: "fc-list : family | grep 'PowerlineSymbols'"
    dependencies:
      - homebrew
      - fontconfig
  macos-work:
    install: brew install --cask font-powerline-symbols
    check: "fc-list : family | grep 'PowerlineSymbols'"
    dependencies:
      - homebrew
      - fontconfig

name: bash
homepage: https://www.gnu.org/software/bash/
description: |
  The GNU Bourne Again SHell — a widely-used POSIX shell and scripting language.

environments:
  macos-home:
    install: echo "Using OS-provided bash"
    check: command -v bash
    dependencies: []
  macos-work:
    install: echo "Using OS-provided bash"
    check: command -v bash
    dependencies: []

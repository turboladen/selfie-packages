---
name: zed
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask zed
    check: brew list --cask zed
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask zed
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask zed
    dependencies:
      - homebrew

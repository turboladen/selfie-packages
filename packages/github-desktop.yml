---
name: github-desktop
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask github
    check: brew list --cask github
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask github
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask github
    dependencies:
      - homebrew

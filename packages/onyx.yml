---
name: onyx
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask onyx
    check: brew list --cask onyx
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask onyx
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask onyx
    dependencies:
      - homebrew

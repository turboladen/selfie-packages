name: marksman
homepage: https://github.com/artempyanykh/marksman
description: |
  Write Markdown with code assist and intelligence in the comfort of
  your favourite editor.
dotfiles:
- source: marksman/config.toml
  target: ~/.config/marksman/config.toml
environments:
  macos-work:
    install: brew install marksman
    check: command -v marksman
    dependencies:
    - homebrew
  arch-home:
    install: sudo pacman -S --noconfirm --needed marksman
    check: command -v marksman
    dependencies: []
  macos-home:
    install: brew install marksman
    check: command -v marksman
    dependencies:
    - homebrew

---
name: neomakelsp
version: "0.1.0"
homepage: 
description: 

_default_environment: &default_env
  install: cargo binstall --no-confirm neomakelsp
  check: command -v neomakelsp
  dependencies:
    - cargo-binstall

environments:
  macos-work:
    <<: *default_env
  arch-home:
    <<: *default_env

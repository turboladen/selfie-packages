---
name: orbstack
version: 0.1.0
homepage: 
description: 

environments:
  macos-home:
    install: brew install --cask orbstack
    check: brew list --cask orbstack
    dependencies:
      - homebrew

  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask orbstack
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask orbstack
    dependencies:
      - homebrew

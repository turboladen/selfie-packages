---
name: postico
version: 0.1.0
homepage: 
description: 

environments:
  macos-work:
    install: brew install --cask postico
    check: ls /Applications/Postico*.app &> /dev/null
    dependencies: [homebrew]

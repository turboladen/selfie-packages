---
name: postico
version: 0.1.0
homepage: 
description: 

environments:
  macos-work:
    install: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew install --cask postico
    check: HOMEBREW_CASK_OPTS="--appdir=~/Applications" brew list --cask postico
    dependencies:
      - homebrew

---
name: slack
version: 0.1.0
homepage: https://slack.com
description: Slack is a collaboration plaform that connects teams and tools.

environments:
  macos-work:
    install: brew install --cask slack
    check: ls /Applications/Slack.app/ &> /dev/null
    dependencies: [homebrew]

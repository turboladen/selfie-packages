name: finder-syntax-highlight
homepage: https://github.com/sbarex/SourceCodeSyntaxHighlight
description: Quick Look extension for highlight source code files on macOS 10.15 and later.
post_install_note: |
  TODO: enable the extension: https://github.com/sbarex/SourceCodeSyntaxHighlight?tab=readme-ov-file#enable-the-quick-look-extension"

environments:
  macos-work:
    install: |
      set -e
      brew install --cask syntax-highlight
    check: brew list --cask syntax-highlight
    dependencies:
      - homebrew
  macos-home:
    install: |
      set -e
      brew install --cask syntax-highlight
    check: brew list --cask syntax-highlight
    dependencies:
      - homebrew

---
name: binaryen
version: 0.1.0
homepage: https://github.com/WebAssembly/binaryen
description: Optimizer and compiler/toolchain library for WebAssembly

environments:
  macos-work:
    install: brew install binaryen
    check: command -v wasm-opt
    dependencies: ["homebrew"]

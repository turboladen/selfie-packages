---
name: tree
version: "0.1.0"
homepage: 
description: 

environments:
  macos-home:
    install: brew install tree
    check: command -v tree
    dependencies:
      - homebrew
  macos-work:
    install: brew install tree
    check: command -v tree
    dependencies:
      - homebrew

